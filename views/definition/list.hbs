    {{> navbar}}
<body id="body-list-img">
    <div class="row" id="list-of">
    {{#each definitions as |definition|}}
        <div class="col-10">
            <div class="card mb-4">
                <img src="{{definition.link}}" class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">Término: {{definition.name}}</h5>
                    <h5 class="card-title">Categoría: {{definition.category}}</h5>
                    <p class="card-text">Descripción: {{definition.description}}</p>
                    <p class="card-text">Ejemplo: {{definition.example}}</p>
                    <p class="card-text">Fuente: <a href="{{definition.link}}">{{definition.link}}</a> </p>
                    <p class="card-text">Autor: {{definition.author}}</p>
                    <a href="list/{{definition.id}}" class="btn btn-primary">Ver detalles</a>
                <form action="/list/{{definition.id}}/delete" method="post">
                    <button type="submit">Borrar ejemplo</button>
                </form>
                </div>
            </div>
        </div>
    {{/each}}
</div>

<div class="row" id="list-of">
    {{#each databases as |database|}}
        <div class="col-10">
            <div class="card mb-4">
                <img src="{{database.link}}" class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">Término: {{database.name}}</h5>
                    <h5 class="card-title">Categoría: {{database.category}}</h5>
                    <p class="card-text">Descripción: {{database.description}}</p>
                    <p class="card-text">Ejemplo: {{database.example}}</p>
                    <p class="card-text">Fuente: <a href="{{database.link}}">{{database.link}}</a> </p>
                    <p class="card-text">Autor: {{database.author}}</p>
                    <a href="list/{{database.id}}" class="btn btn-primary">Ver detalles</a>
                </div>
            </div>
        </div>
    {{/each}}
</div>
</body>
